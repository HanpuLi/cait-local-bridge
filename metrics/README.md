# OSS metrics snapshots

These files are auditable snapshots, not targets and not a substitute for source APIs.

- `contributors.json` is generated by `scripts/contributor-metrics.py`. It counts unique external authors of merged pull requests in the rolling 12-month window and excludes the repository owner and obvious bot accounts.
- `oss-health.json` is generated by `scripts/oss-health.py`. It records repository signals and explicitly reports metrics as unavailable when there is no reliable source.

Refresh them with:

```sh
python scripts/contributor-metrics.py
python scripts/oss-health.py
```

Do not create trivial pull requests, artificial dependents, repeated downloads or other activity merely to change these numbers.
